import styled from 'styled-components';

export const Container = styled.div`
    .card__img {
        height: 150px;
    }

    .card__test {
        white-space: nowrap;
        text-overflow: ellipsis;
        overflow: hidden;
    }

    Button {
        background: teal;
    }
`;
